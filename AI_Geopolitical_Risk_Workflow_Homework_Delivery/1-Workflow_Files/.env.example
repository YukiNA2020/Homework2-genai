# Stage 9/10 LLM configuration template.
# Copy this file to ".env" in the same folder when you are ready to test a real LLM.
# Keep ".env" private. The repository-level .gitignore already excludes it.
# Local LLM runtime configuration.
# This file is for your private API settings and should not be committed.

# Provider routing.
LLM_PROVIDER=deepseek
LLM_MODEL=deepseek-v4-flash
LLM_API_STYLE=openai_compatible

# Fill your real deepseek API key on the line below.
DEEPSEEK_API_KEY= sk

# Official deepseek OpenAI-compatible chat endpoint.
DEEPSEEK_API_ENDPOINT=https://api.deepseek.com/chat/completions

# Optional generic aliases. Leave blank unless you switch to another provider later.
LLM_API_KEY=
LLM_API_ENDPOINT=


# MiniMax's image endpoint can be used with --image-mode auto/online.
MINIMAX_API_KEY = sk
IMAGE_PROVIDER=minimax
IMAGE_MODEL=image-01
MINIMAX_IMAGE_MODEL=image-01
MINIMAX_IMAGE_ENDPOINT=https://api.minimaxi.com/v1/image_generation
IMAGE_API_ENDPOINT=
IMAGE_API_KEY=
IMAGE_ASPECT_RATIO=16:9
IMAGE_RESPONSE_FORMAT=base64
IMAGE_TIMEOUT_SECONDS=90
IMAGE_MAX_RETRIES=1
IMAGE_RETRY_BACKOFF_SECONDS=2.0
IMAGE_PROMPT_OPTIMIZER=true
IMAGE_FORCE_OFFLINE=false
IMAGE_FALLBACK_ON_ERROR=true


# Runtime behavior.
LLM_TIMEOUT_SECONDS=30
LLM_MAX_RETRIES=2
LLM_RETRY_BACKOFF_SECONDS=1.5
LLM_TEMPERATURE=0.2
LLM_MAX_TOKENS=1200

# Safety switches.
# Set this to true only when you want to force offline fallback.
LLM_FORCE_OFFLINE=false

# Keep this true during testing so a failed API call does not break the workflow.
LLM_FALLBACK_ON_ERROR=true
